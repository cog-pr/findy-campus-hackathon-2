# 共有アラーム（仮称: WakeMate）— ハッカソン仕様書（1日スコープ）

**ゴール**: 本日 17:00 までに Cloudflare Workers へデプロイし、審査員が触れるデモ＋プレゼンができる状態にする。  
**方針**: 「他者が自分のためにアラームをかけ、起きたかを確認できる」一本に絞る。それ以外は切る。

---

## 0. 一言ピッチ（審査基準1・4）

> **自分のアラームは自分に甘くなる。大事な朝は、信頼できる人が起こす。**  
> WakeMate は、家族・恋人・ルームメイトが「誰の・いつ」のアラームを共有し、時間になったら画面いっぱいの警報を鳴らし、起きた返事が来るまで見守れる共有アラームです。

**誰の課題**: 大事な日なのに起きられない人（受験・旅行・出勤・登校）と、それを心配する側（親・恋人・同居人）。  
**なぜ既存アラームでは足りない**: 自分設定はスヌーズ地獄／設定忘れ。LINE催促は相手依存で「鳴らない」。  
**独自性**: 「アラームを他人がセットできる」＋「**声を出さないと閉じない**起床確認のクローズドループ」＋ Cloudflare Agents SDK（Durable Objects + Alarms）によるサーバー側スケジュール。DBは使わず、Agentの状態（SQLite内蔵・自動永続化）だけで完結させる。

起床確認は「起きた！」ボタンだけだと寝ぼけたまま反射で押せてしまう。そこで **Workers AI（Whisper）で読み上げ音声を判定**し、口が回らないと通らないようにした。「本当に起きたか」を担保するのがこのプロダクトの核なので、そこにAIを効かせている。

ターゲット例（プレゼン用）
- 母親が職場から、子供の登校アラームをセット
- 彼女が、翌日ディズニーの彼氏にアラームをセット

---

## 1. ユーザーストーリー（Must のみ）

| ID | As a… | I want… | So that… | 優先 |
|----|--------|---------|----------|------|
| US1 | 起こしたい人 | グループに招待して相手を選んでアラームをセットしたい | 大事な朝に確実に起こせる | Must |
| US2 | 起こされる人 | 相手がセットしたアラームが自分の端末で鳴ってほしい | 自分が設定し忘れても起きられる | Must |
| US3 | 起こしたい人 | 「起きた」ボタンで返事が来るか見たい | 本当に起きたか不安を解消できる | Must |
| US4 | 両方 | ワンショット（日付指定）で十分使いたい | デモと初回利用が最短 | Must |
| US5 | 起こしたい人 | 期間指定・無期限の繰り返しもしたい | 登校・出勤の日常サポート | Should（時間余れば） |
| US6 | 起こされる人 | スヌーズ／無視したら相手にエスカレーションしたい | 甘えを断ち切る | Could（デモ用簡易版のみ） |

---

## 2. スコープ定義（19:00 必達）

### 2.1 Must（完成必須）

1. **グループ作成・参加**（招待コード 6桁、ニックネームのみ。本格認証なし）
2. **メンバー一覧**（誰がいるか分かる）
3. **ワンショット共有アラーム作成**  
   - 対象メンバー選択  
   - 日時（タイムゾーンは **Asia/Tokyo 固定**）  
   - メッセージ（任意・短文）  
   - 作成者表示
4. **アラーム発火**  
   - サーバー側スケジュール（Agent の `schedule()` → Durable Object Alarm）  
   - 発火時刻に状態を `fired` へ更新 → 対象がアプリを開いていれば自動配信（WebSocket）でフルスクリーン警報を表示  
   - **Web Push は今回なし**（タブを開いておく前提。理由: 実装工数とデモでの伝わりにくさ）
5. **起床確認**（2経路）  
   - **音声（主）**: 発火時にサーバーが確認フレーズ（早口言葉など）を割り当てて対象者に表示 → 読み上げた音声を **Workers AI `@cf/openai/whisper`** で文字起こし → サーバー側でフレーズと照合し、十分一致していれば `confirmed`。判定はサーバー側のみで行い、クライアントの自己申告は信用しない  
   - **ボタン（フォールバック）**: 従来の「起きた！」ボタンも残す。マイクが使えない環境やデモ事故時でも導線が止まらないようにする  
   - どちらの経路でも作成者の画面に自動反映（同じリアルタイム配信の仕組み）  
   - タイムアウト（例: 発火後 5分）で `未確認` 表示
6. **Cloudflare Workers 本番デプロイ**（公開 URL 固定: `https://findy-campus-hackathon-2.trco0430.workers.dev`）
7. **デモシナリオ 1本**＋スライド（課題→解決→技術→ライブデモ）

### 2.2 Should（時間が余ったらこの順）

1. 繰り返し: `毎日 / 平日` × 終了日 or 無期限
2. 未確認時に作成者へ再通知バナー表示（画面内のみの簡易エスカレーション）
3. アラーム一覧のキャンセル／削除

### 2.3 Won't（今日はやらない）

- ネイティブアプリ / App Store
- Google・LINE ログイン、課金、広告
- 音声通話・ビデオ通話で起こす
- 位置情報・バイタル・カメラ監視
- 複雑な権限（管理者ロール多数）
- 多言語・複数タイムゾーン
- AI による起こしメッセージの自動生成（AIは起床判定にだけ使う。文面生成はやらない）
- **DB（D1等）**: Agent の状態管理（SQLite内蔵・自動永続化）だけで完結させる
- **Web Push**（タブを閉じてても鳴らす）: 画面内フルスクリーン警報＋自動リアルタイム配信で代替。タブを開いておく前提
- PWA インストール誘導: Web Pushをやらないため今回は不要

---

## 3. プロダクト仕様（画面・状態）

### 3.1 画面（最小 4）

1. **ホーム / グループ入口**  
   - 「グループ作成」「コードで参加」  
   - ニックネーム入力 → localStorage に `deviceId` + `displayName`
2. **グループルーム**  
   - メンバー、招待コード、アラーム一覧、新規作成ボタン
3. **アラーム作成**  
   - 対象・日時・メッセージ → 作成
4. **発火 / 起床確認 UI**  
   - 対象: フルスクリーン警報＋音（ブラウザ許可範囲）。確認フレーズと「🎤 読み上げる」ボタン、フォールバックの「起きた！」ボタン  
   - 作成者: `待機中 / 起床済み / 未確認` のステータス  
   - 音声確認の UI 状態（対象者）: `idle`（待機）→ `recording`（録音中）→ `judging`（Whisper 判定中）→ 成功で閉じる / 失敗でフィードバック表示して `idle` に戻る（警報音再開）

### 3.1a UIデザイン方針（実装・スライド共通・変更時はここを正とする）

**前提**: プロダクト判断（音声＝Workers AI Whisper、Agents アーキテクチャ、Must スコープ）は **まを案を正**とする。本節は見た目・端末体験の実装メモであり、アーキテクチャを上書きしない。

| 項目 | 方針 |
|------|------|
| デバイス | **スマホ前提・モバイルファースト**（タッチターゲット ≥48px、縦長1カラム） |
| ビジュアル参照 | `docs/slides/` のピッチスライド（青×橙×緑）に寄せる。今後 UI を変えるときはスライドと仕様のこの節を同時更新する |
| ブランド色 | 青 `#0048B0`（主・信頼）／橙 `#E8700C`（CTA・アクセント）／緑 `#449C58`（起床・完了） |
| 警告色 | **派手な赤** `#E10600`（発火フルスクリーン警報のみ。日常画面では使わない） |
| 背景 | 薄い青灰〜白（フラット1色にしない。ソフトブルーグラデ可） |
| フォント | 読みやすいサンセリフ（例: Figtree + Noto Sans JP）。Inter/Roboto 固定は避ける |
| 警報音 | 対象端末では **確認されるまでループ再生**（スヌーズで止めない）。サーバー状態の `timed_out` とは別レイヤ（音はクライアント、状態は Agent） |
| 音声確認 UI | 確認フレーズを大きく表示 →「🎤 読み上げる」→ 録音中 / 判定中の明示 → 不一致時は文言フィードバック。ボタン確認はフォールバックとして残す |
| 公開 URL | 固定: `https://findy-campus-hackathon-2.trco0430.workers.dev` |

画面とスライドの対応（口頭デモ用）:

| 画面 | スライド参照 |
|------|-------------|
| 入口・ルーム・作成 | 03 Must フロー |
| 発火赤画面＋音声確認 | 03 警報コマ ＋（推奨）AI 説明スライド |
| ブランド締め | 00 / 06 |

### 3.2 アラーム状態機械

```
scheduled → fired → confirmed
                 ↘ timed_out（未確認）
scheduled → cancelled
```

| 状態 | 意味 | UI |
|------|------|-----|
| `scheduled` | 予約中 | 一覧に表示・取消可 |
| `fired` | 発火済み・起床待ち | 対象に警報、作成者は「待機中」 |
| `confirmed` | 音声照合 or ボタンで起床確認済み | 両者に完了表示（音声なら `confirmedByVoice`） |
| `timed_out` | 制限時間超過 | 作成者に「未確認」、対象は再通知可（Should） |
| `cancelled` | 取消 | 一覧から薄く表示 or 非表示 |

### 3.3 データモデル（論理・DBなし）

DB（D1等）は使わない。**グループ1つ＝Cloudflare Agentインスタンス1つ**とし、以下の形をそのまま Agent の `state`（自動でSQLite永続化・接続クライアントへ自動配信）として持つ。Agentインスタンスの名前＝招待コードとすることで、コードから直接そのグループのAgentに接続でき、コード検索用の別ストアも不要になる。

```
GroupAgent state = {
  inviteCode      // 6桁、Agentインスタンス名と同一
  name
  createdAt

  members: {
    [deviceId]: { displayName, joinedAt }
  }

  alarms: {
    [alarmId]: {
      id
      creatorDeviceId
      targetDeviceId
      fireAt (ISO, JST解釈)
      message
      recurrence: none | daily | weekdays   # Mustは none のみ
      endsAt? | indefinite                  # Should
      status: scheduled|fired|confirmed|timed_out|cancelled
      confirmedAt?
      createdAt
      wakePhrase?        # 発火時に割り当てる音声確認フレーズ
      confirmedByVoice?  # 音声で確認できた場合 true（ボタン確認と区別）
    }
  }
}
```

`deviceId` はクライアント生成 UUID を localStorage に保存するのみ（サーバー側に Device テーブルは持たない）。

---

## 4. 技術アーキテクチャ（審査基準2・3）

### 4.1 スタック（意図を一文で言えるように）

このスターター（Hono + Vite + React + **Cloudflare Agents SDK**）にそのまま乗る。追加のDB・通知サービスは導入しない。**Hono は確定事項として最後まで外さない**（審査員に Hono 開発者本人がいるため、ルーティング設計・ミドルウェア構成を丁寧に保つ）。デプロイ先は **Cloudflare Workers で確定**。

| レイヤ | 選定 | 意図（審査で言う言葉） |
|--------|------|------------------------|
| 実行基盤 | **Workers + Vite (Hono + React)** | エッジ1本で API と UI を配信。デプロイ単位が単純 |
| 永続 | **Agent の `state`（内蔵 SQLite・自動永続化）** | DB（D1等）を持たない。グループ1つ＝Agentインスタンス1つで完結 |
| スケジュール | **Agent の `schedule()`（Durable Object Alarms）** | 「そのアラーム専用のタイマー」をサーバーに持たせる。cron ポーリングより正確で、ハッカソンらしい CF らしさ |
| 起床通知 | **Agent の自動 WebSocket 配信** | `setState()` すると接続中の全クライアントに自動反映。手動ポーリングも Web Push も不要 |
| 起床判定 | **Workers AI `@cf/openai/whisper`** | 読み上げ音声を文字起こしして本人が本当に起きているか判定。ボタン連打では突破できない起床確認にする |

**技術チャレンジの主役**:  
`アラーム作成 → GroupAgent の this.schedule(fireAt, "fireAlarm", {alarmId}) → 発火時に this.setState() で status を fired に（同時に確認フレーズを割り当て）→ 接続中の対象・作成者へ自動配信 → 対象が読み上げ → env.AI.run("@cf/openai/whisper") で文字起こし → サーバー側で照合して setState(confirmed) → 作成者へ自動配信`

これが「Cloudflare 開発者プラットフォームをどう使ったか」の回答そのもの。DBなし・Pushなしでも「サーバー側で正確に予定を叩き、リアルタイムに状態を配信する」という核心は失われない。さらに Workers AI を**プロダクトの核心（起床の担保）に効かせている**点で、AIが機能のための機能になっていない。

**音声判定の設計方針**:
- 確認フレーズはサーバー側（GroupAgent）で決定し、`state` 経由で対象者に配信する
- 照合もサーバー側（`confirmAlarmByVoice`）で行い、クライアントの「合ってました」という自己申告は信用しない
- 音声認識の揺れを許容するため、記号・空白除去とカナ正規化をかけた上で **bigram類似度 0.6 以上**を合格とする（完全一致は求めない）
- マイク不可・AI呼び出し失敗時はボタン確認にフォールバックできるため、デモ導線は止まらない

### 4.2 ディレクトリ（実装イメージ・スターター構成に準拠）

```
my-hackathon-app/
  wrangler.jsonc
  src/
    index.tsx           # Hono router（agentsMiddleware をマウント）
    agents/
      group-agent.ts     # GroupAgent（state + schedule + @callable RPC）
    client/
      app.tsx            # 画面切り替え（入口/ルーム/作成/発火確認）
      index.tsx
    style.css
```

### 4.3 GroupAgent の RPC（最小・REST APIは持たない）

`agents` の `@callable()` メソッドとして実装し、クライアントは `useAgent` 経由で呼ぶ。

| メソッド | 説明 |
|--------|------|
| `initGroup(name, creatorDeviceId, displayName)` | グループ作成（Agentインスタンス名＝招待コード） |
| `join(deviceId, displayName)` | 招待コードで参加 |
| `createAlarm(creatorDeviceId, targetDeviceId, fireAt, message)` | アラーム作成 → `schedule()` 登録 |
| `confirmAlarm(deviceId, alarmId)` | 起きた！（ボタン。フォールバック経路） |
| `confirmAlarmByVoice(deviceId, alarmId, audio)` | 音声で起床確認。Whisperで文字起こし → サーバー側で照合 |
| `cancelAlarm(deviceId, alarmId)` | 取消 |

内部ハンドラ（スケジュールから呼ばれる・クライアントからは呼ばない）:
- `fireAlarm({ alarmId })` — status を `fired` に。5分後の `timeoutAlarm` を追加で schedule
- `timeoutAlarm({ alarmId })` — 未確認なら status を `timed_out` に

### 4.4 デモ制約（正直にスライドに書く）

- **前提**: 対象者はアプリのタブを開いておく必要がある（Web Pushなしのため）。デモでは「見守る側が画面を開いておく」ことを明言する
- **デモ端末**: Android Chrome または Desktop Chrome を主。ブラウザ2つ（または2プロファイル）で十分再現可能
- **音**: OS / ブラウザの自動再生制限で無音の可能性あり → **フルスクリーン警報（視覚）**を本命 UX にする
- **マイク**: 音声確認には `localhost` か HTTPS が必要（本番URLはHTTPSなので問題なし）。マイク許可を拒否された場合はボタン確認にフォールバックする
- **ログインなし**: deviceId は端末ローカル。紛失・機種変更は対象外

---

## 5. デモシナリオ（3分・審査基準4）

**物語**: 「明日ディズニーの彼氏」／「朝の登校を職場から見守る母」のどちらか1本に固定。

1. **課題**（30秒）: 自分アラームはスヌーズで負ける。大事な朝は“他人の本気”が要る。  
2. **解決**（30秒）: グループ共有 → 相手にアラームセット → 発火で自動警報 → **声の読み上げ（Whisper）で起床確認**までクローズ。  
3. **ライブ**（90秒）:  
   - 端末Aでグループ作成、端末Bで参加  
   - AがBに「3分後」アラーム作成（審査では短時間）  
   - Bの画面に自動でフルスクリーン警報＋確認フレーズ → **Bが声に出して読み上げる** → Whisperが判定 → Aのステータスが自動で `起床済み` に  
4. **技術＋AI**（30秒）: Workers + **Cloudflare Agents（Durable Objects + Alarms）** の図と、**Workers AI（Whisper）起床判定**の流れ（読み上げ→録音→文字起こし→照合→confirmed）を1〜2枚。DBもPushも使わずサーバー側スケジュール＋リアルタイム配信だけで成立している点と、AIが起床の担保という核心に効いている点を強調。

バックアップ: Wi-Fiが不安定でも、双方タブを開いたままにしておけば同一デモ環境内で確実に再現できる（外部Pushサービス依存がないため）。

---

## 6. タイムボックス（目安・いまから19:00）

| 時間帯 | 成果物 |
|--------|--------|
| 〜12:30 | 骨格: GroupAgent（state定義）+ グループ作成/参加 UI |
| 〜14:30 | アラーム作成 + `schedule()` 発火 + 状態のリアルタイム配信確認 |
| 〜16:00 | 発火時のフルスクリーン警報 UI・「起きた！」・タイムアウト表示 |
| 〜17:00 | デプロイ安定化、デモ通し（2端末/2ブラウザ）、失敗時フォールバック確認 |
| 〜18:00 | スライド・ピッチ原稿・技術図 |
| 〜19:00 | 最終通し＋バッファ |

**カット順（遅延時）**: 繰り返し → エスカレーション → 取消 UI → 見た目磨き →（最後まで残す）発火のリアルタイム配信と起床確認。

---

## 7. 審査基準（公式・各10点満点／計40点）

> **注意**: 審査員に Hono の開発者本人が含まれる。Hono の使い方が雑だと技術チャレンジ・完成度の両方で減点材料になり得るので、素朴でも「意図を持った」Honoの使い方（ルーティング構成・`hono-agents`ミドルウェアでの Agents SDK 連携など）を崩さないこと。Cloudflare Workers へのデプロイは確定事項、CFの機能（Workers / Agents SDK＝Durable Objects + Alarms）を積極的に前面に出す。

| # | 基準 | 観点 |
|---|------|------|
| 1 | 課題と解決策（10点） | 誰のどんな課題を解くのかが伝わるか／アプローチの納得感や独自性 |
| 2 | プロダクト完成度（10点） | メインの機能が動く状態まで完成しているか／Cloudflare Workers にデプロイできているか |
| 3 | 技術チャレンジ（10点） | Cloudflare 開発者プラットフォームをどのように使ったか／新しい技術や難しい実装に挑戦しているか／技術選定の意図が見えるか |
| 4 | プレゼン・熱量（10点） | わかりやすく伝えられているか／チームとしての取り組みが伝わるか |

### この仕様での取り方

| # | この仕様での取り方 |
|---|-------------------|
| 1 | 「自分に甘い朝」×「信頼できる他者」の物語を固定。独自性＝共有セット＋起床クローズドループ |
| 2 | Must 4画面＋本番 URL（Cloudflare Workers）。メイン導線だけ確実に動かす |
| 3 | **Hono**（ルーティング）＋ **Cloudflare Agents SDK**（Durable Objects + Alarms：状態の自動永続化・自動リアルタイム配信）＋ **Workers AI（Whisper）による起床判定**。DBもPushサービスも使わずに成立している点、そしてAIを飾りでなくプロダクトの核心（起床の担保）に効かせている点で技術選定の意図を説明する |
| 4 | 3分シナリオ固定、2端末ライブ、失敗時の口頭リカバリを用意。「なぜこの技術構成にしたか」をチームの言葉で語れるようにする |

---

## 8. 受け入れ条件（Done の定義）

- [ ] 公開 URL（固定: `https://findy-campus-hackathon-2.trco0430.workers.dev`）でグループ作成・参加ができる  
- [ ] 別端末（または別ブラウザプロファイル）にワンショットアラームをセットできる  
- [ ] 発火時刻に対象の開いている画面へフルスクリーン警報が自動で出る（Web Pushなし・タブオープン前提）  
- [ ] 対象が確認フレーズを読み上げると Whisper 判定を通って `起床済み` になり、作成者側に反映される  
- [ ] マイクが使えない場合も「起きた！」ボタンで確認でき、作成者側ステータスに反映される  
- [ ] 上記をスライドのデモ手順どおり 3 分以内に再現できる  

---

## 9. ネーミング候補

- WakeMate（推奨・説明しやすい）
- 起こし隊
- ShareAlarm
- おまかせアラーム

プレゼンでは **WakeMate** で統一推奨。

---

## 10. 他エージェント向け実装メモ

- プロダクト名は **WakeMate** で統一（別名: 起こし隊 / ShareAlarm）
- **公開 URL（固定）**: `https://findy-campus-hackathon-2.trco0430.workers.dev`（`wrangler.jsonc` の `name` = `findy-campus-hackathon-2`。まを のアカウント以外へデプロイしない）
- auth は作らない。`deviceId` はクライアント生成 UUID を localStorage 保存
- **DBは使わない**。Group1つ＝Cloudflare Agentインスタンス1つとし、`state` にメンバー・アラームをJSONで保持（論理モデルは §3.3）。招待コード＝Agentインスタンス名
- **Web Pushは使わない**。Agentの`setState()`による自動WebSocket配信でリアルタイム反映する
- **AIは起床判定にだけ使う**（`@cf/openai/whisper`）。文面生成などには広げない。判定は必ずサーバー側で行い、クライアントの自己申告を信用しない
- 音声確認を足してもボタン確認は消さない。デモ中にマイクが使えなくても導線が止まらないようにするため
- 繰り返し・無期限はstateの型に枠だけ残し、UI/ロジックは後回しでよい
- まず「3分後アラーム」が2端末（2ブラウザプロファイルでも可）で通ることを最優先

質問・仕様変更がある場合は、Must を減らす方向でのみ調整すること（増やさない）。

UI・色・モバイル警報の見た目を変えるときは **§3.1a を先に更新**してからコード／スライドを揃える。音声・Whisper・Agent 設計を変えるときは **まを案（§2.1-5 / §4）を正**とし、勝手にクライアント音声認識へ戻さない。

---

## 11. 衝突メモ（まを案 vs UIブランチ実装メモ）— 上書き禁止一覧

仕様・プロダクトの正は **まを**。UIブランチ（rikuto 側の見た目・モバイル警報）は実装ノートとして取り込むが、まをの製品判断を黙って消さない。

| # | 項目 | まを（正・仕様） | 俺ら / UIブランチ側に残りがちなもの | 扱い |
|---|------|------------------|--------------------------------------|------|
| 1 | **起床音声の方式** | **Workers AI `@cf/openai/whisper`**。録音 → サーバー文字起こし → フレーズ照合（`confirmAlarmByVoice`） | クライアント `webkitSpeechRecognition`（`alarmVoice.ts` がレガシーとして残存しうる） | **Whisper を正**。SpeechRecognition は採用しない／復活させない |
| 2 | **確認フレーズ** | サーバーが発火時に `wakePhrase` を割当。早口言葉など読み上げ必須 | 「起きた」「I'm awake」など自由発話のクライアント辞書マッチ | **wakePhrase 読み上げを正** |
| 3 | **判定の場所** | サーバーのみ（クライアント自己申告を信用しない） | ブラウザ認識結果でそのまま `confirmAlarm` | **サーバー照合を正** |
| 4 | **発火画面の色** | 仕様は「フルスクリーン警報」（色指定はもともと弱い） | スライド寄りの緑起床シグナル／旧ダーク夜明けパレット（`presentation/DESIGN_NOTES` の `#0a0e18`×`#ff4d1a`×`#12b89a`） | **実装 UI は派手赤 `#E10600` + 青橙緑ブランド（§3.1a）**。スライド再制作時も赤警報に合わせる |
| 5 | **警報の長さ** | サーバーは発火後タイムアウトで `timed_out`（例: 5分） | クライアント音は確認まで無限ループ | **両立**: 音は確認までループ、状態機械の `timed_out` はまをどおり残す |
| 6 | **AI の位置づけ** | AIは起床判定の核（Whisper）。文面生成はやらない | 旧ピッチ構成に「AIを切った」と書く箇所あり（`presentation/STRUCTURE` 等） | **仕様・新スライドでは Whisper を前面に**。「AIを切った」は古い。Agents SDK は状態・スケジュールの基盤（生成AIではない） |
| 7 | **デプロイ／ブランチ** | 公開 URL 固定・まをアカウントの Worker。main への仕様は PR 経由 | UIブランチからの直接 main マージや別アカウントデプロイ | **URL・アカウントはまを正**。本ドキュメント変更は feature ブランチ＋PR（自動マージしない） |
| 8 | **ボタン確認** | フォールバックとして残す | 音声のみにする案／逆にボタンだけにする案 | **両方残す（まを）** |

衝突を解消するときは、表の「まを」列を仕様本文に残し、UI列は §3.1a や実装メモに retro する。
