# WakeMate（共有アラーム）— 共有用サマリー

ハッカソン用。**本日17:00までに Workers デプロイ＋デモ＋プレゼン**がゴール。  
詳細正本: `projects/shared-alarm/SPEC.md`

---

## ひとこと

**自分のアラームは自分に甘くなる。大事な朝は、信頼できる人が起こす。**

家族・恋人・同居人が「誰に・いつ」アラームをセットし、相手の端末を鳴らし、「起きた！」まで確認できる Web アプリ。

---

## 誰の・どんな課題か

| 誰 | 課題 |
|----|------|
| 起きられない人 | 大事な日（受験・旅行・出勤・登校）なのにスヌーズ地獄／設定忘れ |
| 起こしたい人（親・恋人など） | LINE催促だけでは「端末が鳴らない」。本当に起きたか分からない |

**既存アラームとの差**: 自分設定に依存しない。**他人がセットできる＋起床確認まで閉じる**のが独自性。

ターゲット例（プレゼン用）
- 母親が職場から、子供の登校アラームをセット
- 彼女が、翌日ディズニーの彼氏にアラームをセット

---

## 今日作るもの（Must）／作らないもの

### Must（これだけ完成させる）
1. 招待コードでグループ作成・参加（ニックネームのみ。本格ログインなし）
2. ワンショット共有アラーム（対象メンバー・日時・メッセージ）※タイムゾーンは **JST 固定**
3. 発火: サーバー側スケジュール → 対象へ **Web Push**（開いていれば画面警報も）
4. 「起きた！」ボタン → 作成者側ステータス更新（制限時間後は「未確認」）
5. **Cloudflare Workers 本番デプロイ**＋2端末デモ＋スライド

### Should（余力があればこの順）
- 繰り返し（毎日／平日 × 終了日 or 無期限）
- 未確認時に作成者へリマインド Push
- アラーム取消 UI
- PWA インストール誘導（iOS はホーム画面追加が Push 前提、と明記）

### Won't（今日はやらない）
ネイティブアプリ / OAuth / 通話で起こす / 位置情報 / 複雑な権限 / 複数タイムゾーン

---

## 画面（最小4）

1. **入口** — グループ作成 or コード参加、ニックネーム
2. **グループルーム** — メンバー、招待コード、アラーム一覧
3. **アラーム作成** — 対象・日時・メッセージ
4. **発火／起床確認** — 対象は大きな「起きた！」、作成者は `待機中 / 起床済み / 未確認`

### アラーム状態
`scheduled` → `fired` → `confirmed`  
　　　　　　　↘ `timed_out`  
`scheduled` → `cancelled`

---

## 技術スタック（意図つき）

| 部品 | 何に使うか | 審査で言う意図 |
|------|------------|----------------|
| Workers + Assets | API と SPA を1デプロイ | 完成度・再現性 |
| D1 | グループ・メンバー・アラーム | 関係データを SQL で持つ |
| **Durable Objects + Alarm API** | アラーム単位のサーバータイマー | **技術チャレンジの主役** |
| Web Push (VAPID) | タブ閉じてても端末を起こす | プロダクトの核心 |

**核の流れ**  
アラーム作成 → 対象用 DO に `setAlarm(fireAt)` → 発火で D1 更新 + Web Push → 「起きた！」でステータス確定

### 最小 API
- `POST /api/devices` — deviceId / 名前 / Push購読
- `POST /api/groups` — グループ作成 → 招待コード
- `POST /api/groups/join` — 参加
- `GET /api/groups/:id` — メンバー＋アラーム一覧
- `POST /api/groups/:id/alarms` — アラーム作成（DO に schedule）
- `POST /api/alarms/:id/confirm` — 起きた
- `GET /api/vapid-public-key` — クライアント購読用
- （Should）`DELETE /api/alarms/:id` — 取消

### デモ時の制約（正直に共有）
- 主戦場: Android Chrome / Desktop Chrome
- iOS: ホーム画面追加後のみ Push 想定
- 音は OS/ブラウザ次第 → **通知バナー＋全画面警報**を本命 UX にする

---

## 審査基準への当て方

1. **課題と解決策** — 「自分に甘い朝 × 信頼できる他者」の物語を1本に固定
2. **完成度** — Must 導線が本番 URL で動くこと（繰り返しは不要）
3. **技術チャレンジ** — DO Alarms + D1 + Web Push をセットで説明
4. **プレゼン** — 3分シナリオ固定、2端末ライブ、Push失敗時はタブ警報でリカバリ

### 3分デモ台本
1. 課題 30秒 → 2. 解決 30秒 → 3. ライブ 90秒（作成→参加→短時間アラーム→起きた→ステータス）→ 4. 技術図 30秒

---

## タイムボックス（〜19:00）

| まで | ゴール |
|------|--------|
| 12:30 | グループ作成／参加 |
| 14:30 | DO Alarm 発火まで |
| 16:00 | Push ＋「起きた！」 |
| 17:00 | デプロイ＆2端末通し |
| 18:00 | スライド |
| 19:00 | 最終通し |

**遅れたら切る順**: 繰り返し → エスカレーション → 取消 → 見た目  
**最後まで残す**: Web Push と起床確認

---

## Done の定義

- [ ] 公開 URL でグループ作成・参加できる
- [ ] 別端末（or 別ブラウザ）にワンショットアラームをセットできる
- [ ] 発火時に対象へ通知 or アプリ内警報が出る
- [ ] 「起きた！」が作成者ステータスに反映される
- [ ] 上記を3分以内のデモ手順で再現できる

---

## 他エージェント向け実装メモ

- プロダクト名は **WakeMate** で統一（別名: 起こし隊 / ShareAlarm）
- auth は作らない。`deviceId` はクライアント生成 UUID を localStorage 保存
- データ: Device / Group / Membership / Alarm（論理モデルは SPEC.md §3.3）
- 繰り返し・無期限はスキーマに枠だけ残し、UI/ロジックは後回しでよい
- まず「3分後アラーム」が2端末で通ることを最優先

質問・仕様変更がある場合は、Must を減らす方向でのみ調整すること（増やさない）。
