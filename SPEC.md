# 共有アラーム（仮称: WakeMate）— ハッカソン仕様書（1日スコープ）

**ゴール**: 本日 19:00 までに Cloudflare Workers へデプロイし、審査員が触れるデモ＋プレゼンができる状態にする。  
**方針**: 「他者が自分のためにアラームをかけ、起きたかを確認できる」一本に絞る。それ以外は切る。

---

## 0. 一言ピッチ（審査基準1・4）

> **自分のアラームは自分に甘くなる。大事な朝は、信頼できる人が起こす。**  
> WakeMate は、家族・恋人・ルームメイトが「誰の・いつ」のアラームを共有し、Web Push で端末を叩き、起きた返事が来るまで見守れる共有アラームです。

**誰の課題**: 大事な日なのに起きられない人（受験・旅行・出勤・登校）と、それを心配する側（親・恋人・同居人）。  
**なぜ既存アラームでは足りない**: 自分設定はスヌーズ地獄／設定忘れ。LINE催促は相手依存で「鳴らない」。  
**独自性**: 「アラームを他人がセットできる」＋「起床確認のクローズドループ」＋ Cloudflare Durable Objects Alarms によるサーバー側スケジュール。

---

## 1. ユーザーストーリー（Must のみ）

| ID | As a… | I want… | So that… | 優先 |
|----|--------|---------|----------|------|
| US1 | 起こしたい人 | グループに招待して相手を選んでアラームをセットしたい | 大事な朝に確実に起こせる | Must |
| US2 | 起こされる人 | 相手がセットしたアラームが自分の端末で鳴ってほしい | 自分が設定し忘れても起きられる | Must |
| US3 | 起こしたい人 | 「起きた」ボタンで返事が来るか見たい | 本当に起きたか不安を解消できる | Must |
| US4 | 両方 | ワンショット（日付指定）で十分使いたい | デモと初回利用が最短 | Must |
| US5 | 起こしたい人 | 期間指定・無期限の繰り返しもしたい | 登校・出勤の日常サポート | Should（時間余れば） |
| US6 | 起こされる人 | スヌーズ／無視したら相手にエスカレーションしたい | 甘えを断ち切る | Could（デモ用簡易版のみ） |

---

## 2. スコープ定義（19:00 必達）

### 2.1 Must（完成必須）

1. **グループ作成・参加**（招待コード 6桁、ニックネームのみ。本格認証なし）
2. **メンバー一覧**（誰がいるか分かる）
3. **ワンショット共有アラーム作成**  
   - 対象メンバー選択  
   - 日時（タイムゾーンは **Asia/Tokyo 固定**）  
   - メッセージ（任意・短文）  
   - 作成者表示
4. **アラーム発火**  
   - サーバー側スケジュール → 対象端末へ **Web Push**  
   - アプリ開いていれば画面内モーダル／フルスクリーン警報も同時
5. **起床確認**  
   - 対象者が「起きた！」を押す → 作成者の画面／Push でステータス更新  
   - タイムアウト（例: 発火後 5分）で `未確認` 表示
6. **Cloudflare Workers 本番デプロイ**（公開 URL）
7. **デモシナリオ 1本**＋スライド（課題→解決→技術→ライブデモ）

### 2.2 Should（時間が余ったらこの順）

1. 繰り返し: `毎日 / 平日` × 終了日 or 無期限
2. 未確認時に作成者へリマインド Push（簡易エスカレーション）
3. アラーム一覧のキャンセル／削除
4. PWA インストール誘導（iOS はホーム画面追加が Push 前提である旨を UI に明記）

### 2.3 Won't（今日はやらない）

- ネイティブアプリ / App Store
- Google・LINE ログイン、課金、広告
- 音声通話・ビデオ通話で起こす
- 位置情報・バイタル・カメラ監視
- 複雑な権限（管理者ロール多数）
- 多言語・複数タイムゾーン
- AI 自動メッセージ生成（余談ネタに留める）

---

## 3. プロダクト仕様（画面・状態）

### 3.1 画面（最小 4）

1. **ホーム / グループ入口**  
   - 「グループ作成」「コードで参加」  
   - ニックネーム入力 → localStorage に `deviceId` + `displayName`
2. **グループルーム**  
   - メンバー、招待コード、アラーム一覧、新規作成ボタン
3. **アラーム作成**  
   - 対象・日時・メッセージ → 作成
4. **発火 / 起床確認 UI**  
   - 対象: 大きな「起きた！」ボタン＋音（ブラウザ許可範囲）  
   - 作成者: `待機中 / 起床済み / 未確認` のステータス

### 3.2 アラーム状態機械

```
scheduled → fired → confirmed
                 ↘ timed_out（未確認）
scheduled → cancelled
```

| 状態 | 意味 | UI |
|------|------|-----|
| `scheduled` | 予約中 | 一覧に表示・取消可 |
| `fired` | 発火済み・起床待ち | 対象に警報、作成者は「待機中」 |
| `confirmed` | 起きた押下済み | 両者に完了表示 |
| `timed_out` | 制限時間超過 | 作成者に「未確認」、対象は再通知可（Should） |
| `cancelled` | 取消 | 一覧から薄く表示 or 非表示 |

### 3.3 データモデル（論理）

```
Device
  id (uuid, client生成)
  displayName
  pushSubscription? (endpoint, p256dh, auth)
  updatedAt

Group
  id
  inviteCode (6桁)
  name
  createdAt

Membership
  groupId
  deviceId
  joinedAt

Alarm
  id
  groupId
  creatorDeviceId
  targetDeviceId
  fireAt (ISO, JST解釈)
  message
  recurrence: none | daily | weekdays   # Mustは none のみ
  endsAt? | indefinite                  # Should
  status: scheduled|fired|confirmed|timed_out|cancelled
  confirmedAt?
  createdAt
```

---

## 4. 技術アーキテクチャ（審査基準2・3）

### 4.1 スタック（意図を一文で言えるように）

| レイヤ | 選定 | 意図（審査で言う言葉） |
|--------|------|------------------------|
| 実行基盤 | **Workers + Assets (SPA)** | エッジ1本で API と UI を配信。デプロイ単位が単純 |
| 永続 | **D1** | グループ・メンバー・アラームの関係データを SQL で扱う |
| スケジュール | **Durable Objects + `Alarm` API** | 「そのアラーム専用のタイマー」をサーバーに持たせる。cron ポーリングより正確で、ハッカソンらしい CF らしさ |
| 通知 | **Web Push (VAPID) from Worker** | タブ閉じていても端末を起こす。完成度の核心 |
| リアルタイム（任意） | DO WebSocket or 短い polling | 起床ステータスを作成者画面に即反映 |

**技術チャレンジの主役**:  
`Alarm 作成 → 対象 Alarm 用 DO に `storage.setAlarm(fireAt)` → 発火時に D1 更新 + Web Push 送信 +（任意）作成者へ状態配信`

これが「Cloudflare 開発者プラットフォームをどう使ったか」の回答そのもの。

### 4.2 推奨ディレクトリ（実装イメージ）

```
wake-mate/
  wrangler.toml
  src/
    index.ts          # Hono router
    db/schema.sql
    alarm-do.ts       # Durable Object (setAlarm / alarm())
    push.ts           # VAPID + webcrypto web-push
    types.ts
  public/ or client/  # 最小 SPA（素の HTML/JS か Vite）
  presentations/      # スライド素材メモ
```

### 4.3 API（最小）

| Method | Path | 説明 |
|--------|------|------|
| POST | `/api/devices` | deviceId / name / push subscription upsert |
| POST | `/api/groups` | グループ作成 → inviteCode |
| POST | `/api/groups/join` | code + deviceId |
| GET | `/api/groups/:id` | メンバー＋アラーム一覧 |
| POST | `/api/groups/:id/alarms` | アラーム作成（DO に schedule） |
| POST | `/api/alarms/:id/confirm` | 起きた |
| DELETE | `/api/alarms/:id` | 取消（Should） |
| GET | `/api/vapid-public-key` | クライアント購読用 |

### 4.4 デモ制約（正直にスライドに書く）

- **デモ端末**: Android Chrome または Desktop Chrome を主。iOS は「ホーム画面追加後のみ Push」と明記。
- **音**: OS / ブラウザの制限で無音の可能性あり → **通知バナー＋画面フル警報**を本命 UX にする。
- **ログインなし**: deviceId は端末ローカル。紛失・機種変更は対象外。

---

## 5. デモシナリオ（3分・審査基準4）

**物語**: 「明日ディズニーの彼氏」／「朝の登校を職場から見守る母」のどちらか1本に固定。

1. **課題**（30秒）: 自分アラームはスヌーズで負ける。大事な朝は“他人の本気”が要る。  
2. **解決**（30秒）: グループ共有 → 相手にアラームセット → Push で鳴る → 「起きた！」でクローズ。  
3. **ライブ**（90秒）:  
   - 端末Aでグループ作成、端末Bで参加  
   - AがBに「3分後」アラーム作成（審査では短時間）  
   - Bに Push／警報 → Bが「起きた！」→ Aのステータスが `起床済み`  
4. **技術**（30秒）: Workers + D1 + **DO Alarms** + Web Push の図1枚。

バックアップ: Push が現場 Wi-Fi で落ちても、開いたタブの警報＋ポーリングで「起きた」まで見せる。

---

## 6. タイムボックス（目安・いまから19:00）

| 時間帯 | 成果物 |
|--------|--------|
| 〜12:30 | 骨格: Workers + D1 schema + グループ作成/参加 UI |
| 〜14:30 | アラーム作成 + Durable Object `setAlarm` + 発火で D1 更新 |
| 〜16:00 | Web Push 購読・送信・発火 UI・「起きた！」 |
| 〜17:00 | デプロイ安定化、デモ通し（2端末）、失敗時フォールバック確認 |
| 〜18:00 | スライド・ピッチ原稿・技術図 |
| 〜19:00 | 最終通し＋バッファ |

**カット順（遅延時）**: 繰り返し → エスカレーション → 取消 UI → 見た目磨き →（最後まで残す）Push と起床確認。

---

## 7. 審査採点へのマッピング

| 基準 | この仕様での取り方 |
|------|-------------------|
| 1 課題と解決策 | 「自分に甘い朝」×「信頼できる他者」の物語を固定。独自性＝共有セット＋起床クローズドループ |
| 2 完成度 | Must 4画面＋本番 URL。メイン導線だけ確実に動かす |
| 3 技術チャレンジ | DO Alarms を主役に据える。D1・Web Push をセットで「なぜ CF か」を説明 |
| 4 プレゼン熱量 | 3分シナリオ固定、2端末ライブ、失敗時の口頭リカバリを用意 |

---

## 8. 受け入れ条件（Done の定義）

- [ ] 公開 URL でグループ作成・参加ができる  
- [ ] 別端末（または別ブラウザプロファイル）にワンショットアラームをセットできる  
- [ ] 発火時刻に対象へ通知 or アプリ内警報が出る  
- [ ] 対象の「起きた！」が作成者側ステータスに反映される  
- [ ] 上記をスライドのデモ手順どおり 3 分以内に再現できる  

---

## 9. ネーミング候補

- WakeMate（推奨・説明しやすい）
- 起こし隊
- ShareAlarm
- おまかせアラーム

プレゼンでは **WakeMate** で統一推奨。
